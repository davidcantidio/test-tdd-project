# 🧪 Development Environment Configuration
# TDD Framework - Development Settings

[app]
name = "TDD Framework (Dev)"
environment = "development"
debug = true
version = "1.0.0-dev"
host = "localhost"
port = 8501

[database]
framework_db_path = "framework.db"
timer_db_path = "task_timer.db"
connection_timeout = 10
pool_size = 3
max_overflow = 5

# Development database optimizations
enable_wal_mode = false  # Simpler for dev
cache_size = -1000  # 1MB cache (smaller for dev)
temp_store = "memory"

[security]
require_auth = false  # Optional auth for development
csrf_token_expiry = 7200  # 2 hours (longer for dev convenience)
session_timeout_minutes = 480  # 8 hours
cookie_name = "tdd_framework_session_dev"

# Relaxed security for development
enable_rate_limiting = false
enable_dos_protection = false
enable_log_sanitization = true
log_level = "DEBUG"

[performance]
enable_database_cache = true
cache_ttl_seconds = 60  # Short TTL for development (1 minute)
enable_redis = false  # No Redis in development
redis_url = ""

# Pagination settings
default_page_size = 10  # Smaller pages for easier testing
max_page_size = 100

# Streamlit optimization
enable_streamlit_cache = true

[monitoring]
enable_health_check = false  # Not needed in development
health_check_port = 8080
enable_metrics = false
metrics_port = 9090

# Development logging
log_format = "text"  # Human-readable logs
enable_correlation_ids = false  # Simpler logging
log_file_path = ""  # Console logging only

[google_oauth]
# OAuth credentials loaded from environment variables (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# OAuth will be automatically enabled/disabled based on credential availability
# 
# To enable OAuth in development:
# 1. Set environment variables: export GOOGLE_CLIENT_ID="your_id" && export GOOGLE_CLIENT_SECRET="your_secret"  
# 2. OR create .streamlit/secrets.toml with credentials
# 3. OAuth will auto-enable when credentials are detected
#
# Without credentials: OAuth is disabled, development fallback authentication is used
client_id = "${GOOGLE_CLIENT_ID}"  # Auto-loaded from environment
client_secret = "${GOOGLE_CLIENT_SECRET}"  # Auto-loaded from environment  
redirect_uri = "http://localhost:8501"
# enabled = dynamic  # Automatically set based on credential availability
scopes = [
    "https://www.googleapis.com/auth/userinfo.email", 
    "https://www.googleapis.com/auth/userinfo.profile"
]